# Local secrets — never commit
.env
.env.local

# Local-only agent settings
.claude/settings.local.json

# Runtime-cloned zsh plugin (installed by fresh.sh)
plugins/fzf-tab/

# Zed prompt-library state DB (binary LMDB, mutable runtime state — not config)
config/zed/prompts/

# macOS
.DS_Store
