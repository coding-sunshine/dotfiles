---
paths:
  - "**/*.py"
---

# Python

- Use `uv` for envs/deps (`uv run`, `uv add`, `uv sync`).
- Lint/format with `ruff` (`ruff check --fix`, `ruff format`).
- Add type hints on public functions; keep modules small and focused.
- Reuse existing utilities; match the project's layout and naming.
