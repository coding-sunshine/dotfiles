---
paths:
  - "**/*.php"
---

# PHP / Laravel

- Run artisan/composer/php via Herd: `herd php artisan ...`.
- Tests: `pest --no-coverage`. Format: Pint (`vendor/bin/pint`).
- Follow framework conventions (form requests, actions, Eloquent scopes, events).
- Reuse existing services/helpers; don't reinvent what the framework provides.
